# 카카오 상담 — 전화번호 기반 승인 보조 계획

> 목표(사용자 정의): **승인 게이트는 유지한다.** 관리자가 가입자가 정확히 어느 고객사
> 직원인지 확인하고 승인해야 한다. 다만 그 확인을 돕도록 **전화번호를 확보해, 전화 확인 후
> 고객사 매핑 및 승인**을 처리하는 흐름으로 개선한다.
>
> 작성: 2026-08-24. 상태: **P1 로컬 구현 완료(미배포)** — 웹훅 전화 자동수집(A)+승인화면 후보제안. 관련: KAKAO_JOURNEY_PLAN.md(K1~K3),
> CR-3(연락처 승인 게이트 — **유지**), 상담 관제(chat)·KakaoContact 모델(phone 필드 존재).

---

## 0. 성공 기준
- 가입자가 채널에 말을 걸면, **승인 대기 화면에 전화번호가 함께** 보인다.
- 그 전화번호로 고객사 연락처를 대조해 **"일치 후보 고객사"를 관리자에게 제시**한다
  (제안일 뿐, 확정은 사람이).
- 관리자는 전화로 신원을 확인한 뒤 **한 번의 조작으로 고객사 매핑 + 승인**을 끝낸다(CR-3 유지).
- 승인 이후 채팅 흐름(스레드·핸드오프)은 기존과 동일.

## 1. 현행과 개선점
현행: 미등록 가입자가 말을 걸면 PENDING 등록되나 관리자가 가진 정보는 **닉네임·첫 발화뿐**이라
"어느 회사 누구인지" 확인이 어렵다. 승인 시 client_id를 수동으로 찾아 매핑한다.

개선: **전화번호를 확보**해 (1) 승인 화면에 노출하고 (2) 고객사 연락처와 대조해 **후보를
자동 제안**한다. 승인 판단·확정은 **여전히 사람**이 한다(사칭 방지 그대로).

## 2. 전화번호 확보 방법 — 두 경로
### A. 카카오 동의 기반 자동 수집 (권장, 확인 필요)
- 채널 **정보이용 동의**에 전화번호를 포함시키고, 오픈빌더 사용자정보/스킬 파라미터로
  전화번호를 받는다. 첫 웹훅에서 `KakaoContact.phone`에 저장.
- **전제(확인 필요)**: 우리 채널이 전화번호 수집 동의를 받도록 설정돼 있고, 스킬/사용자정보
  API로 전달되는가(카카오 i.openbuilder·채널 관리자 설정 사안). 스킬 기본 payload엔
  닉네임만 오므로, 이 경로 가부를 **카카오 콘솔에서 먼저 확인**해야 한다.

### B. 챗봇 자가 입력 (폴백, 카카오 설정 불요)
- 미등록 가입자에게 봇이 안내: "상담 연결을 위해 **회사명과 담당자 전화번호**를 남겨주세요."
- 사용자가 입력한 전화/회사명을 파싱해 `KakaoContact.phone`·name·memo에 저장.
- 자가 신고라 신뢰도는 낮지만, **관리자가 대조·전화 확인할 근거**는 확보된다.
- A가 안 되는 환경에서도 즉시 동작. A 도입 후에도 보조로 병행 가능.

## 3. 승인 화면 보강 (핵심)
- **승인 대기 목록/상세에 전화번호 표시** + 첫 발화·닉네임.
- 전화번호 **정규화 후 고객사 연락처 대조**(Client.main_contact_phone·ceo_contact_phone,
  필요 시 수신자 전화):
  - 1곳 일치 → **"제안: ○○운수 (주담당 전화 일치)"** 배지 + [이 고객사로 승인] 버튼.
  - 여러 곳/0곳 → 후보 목록 또는 "일치 없음" 표시 → 관리자가 검색해 매핑.
- 제안은 **어디까지나 보조** — 최종 매핑·승인은 관리자 클릭(전화 확인 후). CR-3 불변.
- 승인 처리는 기존 `update_kakao_contact`(status=APPROVED + client_id 확정 + KAKAO_APPROVAL
  감사) 재사용. 전화 제안은 입력을 채워주는 UI 보조일 뿐 새 승인 경로가 아니다.

## 4. 데이터·코드 영향(개요)
- `KakaoContact.phone`: **이미 존재** — 스키마 변경 없이 채우기만 하면 됨.
- (선택) `verification_hint`/제안 후보는 서버 계산으로 응답에 실어 보냄(저장 불필요).
- webhook(미등록 분기): (A) 전화 파라미터 수신 시 phone 저장, (B) 자가입력 유도 문구·파싱.
- 전화 정규화: 기존 phone 정규화 유틸 재사용(하이픈/국번 통일) → 고객사 전화 인덱스 대조.
- 승인 화면(상담 관제/계정 점검): 전화·후보 제안·[이 고객사로 승인] 액션.
- 감사: 승인은 기존 `KAKAO_APPROVAL` 유지(전화는 비밀값 아님이나 로그엔 필요한 범위만).

## 5. 결정 필요(착수 전 확정)
| # | 질문 | 권고 |
|---|---|---|
| D1 | 카카오 채널이 **전화번호 수집·전달**을 지원하는가(동의 설정) | 콘솔 확인. 되면 A 주경로 |
| D2 | A가 불가하면 **챗봇 자가입력(B)** 로 시작할까 | 예 — 관리자 확인 근거는 확보, 즉시 도입 가능 |
| D3 | 후보 대조 전화 필드 범위 | 우선 주담당·대표 연락처, 필요 시 수신자까지 |
| D4 | 여러 후보 일치 시 | **자동 확정 금지** — 후보 목록만 제시, 사람이 선택 |
| D5 | 승인 전 PENDING 상태에서 **가입자 메시지를 관리자에게 보일까** | 첫 발화만 노출(기존)로 충분 — 필요 시 확장 |

## 6. 단계
1. **P1 승인 화면 보강 — ✅ 로컬 완료**: 웹훅 전화 자동수집(_extract_phone, 페이로드
   후보 경로 방어적) + KakaoContact.phone 저장 + 승인 대기 응답에 전화 대조 후보
   (services/phone_match) + 프론트 [○○ 로 승인] 원클릭. 자동승인 없음(CR-3 유지).
   backend 806·vitest 121 통과.
2. **P2 카카오 콘솔 확정** — 실제 채널이 전화번호를 어느 payload 키로 보내는지 확인 후
   _extract_phone 경로를 좁히고 dev e2e. (A 동의 설정이 안 돼 있으면 B 자가입력 폴백 병행.)
3. 각 단계 로컬 커밋, dev 검증, 지시 시 배포.

## 7. 원칙 재확인
- **승인 게이트(CR-3)는 유지** — 자동 승인·무승인 없음. 사람이 신원을 확인하고 승인한다.
- 전화번호는 **승인을 돕는 근거·UI 보조**일 뿐, 승인 자체를 대체하지 않는다.
- 포털(자기 데이터 열람) 접근은 이 계획과 무관 — 기존 매직링크/K1 관문 유지.
