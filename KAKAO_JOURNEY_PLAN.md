# 카카오 채널 고객 여정 계획 — 가입 → 포털 → 상담 → 이슈 → 해결

> 목표 여정(사용자 정의): 카카오 비즈니스 채널 가입 → (고객사 매칭) → 그걸로 포털 로그인
> → 자기 감축사업 정보 열람 → 문의는 카카오 상담 채팅 → 답변 확인 → 미해결 시 이슈 등록
> → 실무 담당자가 상담 관제에서 대응·회신.
>
> 작성: 2026-08-21. 상태: **설계안(구현 전)**. 관련: 부록 N.8(포털), CR-3(연락처 승인),
> auth-simplified(카카오 접점 일원화), 포털 이용권(1d/7d/30d/365d).

---

## 0. 현행 실사 — 여정 단계별 있음/없음

| # | 여정 단계 | 현행 | 판정 |
|---|---|---|---|
| 1 | 채널 가입(챗봇 진입) | 카카오 웹훅 → KakaoContact 자동 수집 | ✅ 있음 |
| 2 | 고객사 매칭 | 담당자가 연락처 **승인**하며 client 지정(CR-3) | ✅ 있음(수동 승인 — 보안상 유지) |
| 3 | **채널 가입자 포털 진입** | 담당자가 포털 계정을 **수동 발급**해야 링크 수신 | ❌ **없음** — 셀프 요청 경로 부재 |
| 4 | 포털에서 감축사업 열람 | 참여 사업·계약대수·보고서·정산(P1) | ✅ 있음 |
| 5 | **포털 → 카카오 문의 진입** | 포털에 문의 버튼 없음 | ❌ **없음** |
| 6 | 카카오 채팅 문의(AI→담당자) | 챗봇 AI 응대, "상담/담당자" 발화 시 핸드오프(WAITING) → 상담 관제 답변(+알림톡) | ✅ 있음 |
| 7 | **미해결 → 이슈 등록** | 상담 관제에 이슈 승격 기능 없음(이슈 보드에 별도 수동 입력) | ❌ **없음** — 스레드↔이슈 연결 부재 |
| 8 | 이슈 처리 → 상담 회신 | 답변(reply)은 있으나 이슈에서 원 스레드로 돌아가는 연결 없음 | ⚠️ 반쪽 |

**결론**: 양 끝(채널 수집·승인 / 포털 열람 / 상담 응대)은 완성돼 있고, **연결 고리 3개(3·5·7)**가
빠져 있다. 이 3개를 잇는 것이 이번 계획의 전부다 — 새 시스템이 아니라 기존 블록의 배선.

## 1. 설계 원칙
- **승인 게이트 유지**: 채널 가입만으로 자동 매칭·자동 로그인은 하지 않는다(사칭 방지).
  담당자의 연락처 승인(CR-3)이 신원 확인점 — 이후부터는 자동화한다.
- **기존 블록 재사용**: 포털 발급(이용권·매직링크·알림톡), KakaoContact 브릿지, 핸드오프,
  이슈(ActivityHistory ISSUE)를 그대로 쓰고 연결만 추가.
- 모든 연결 지점은 [자동] 활동 이력 + 감사 로그(기존 원칙).

## 2. 단계별 계획 (K1~K4)

### K1. 챗봇 '포털 접속' 셀프 서비스 — 여정 3번 연결
- 챗봇 발화/버튼 **"포털"** 추가(HANDOFF_KEYWORDS 옆 PORTAL_KEYWORDS).
- 웹훅 처리:
  - 연락처 **APPROVED + client 매칭됨** →
    - 포털 계정 있으면: 매직링크 재발급 → **알림톡 즉시 발송**("포털 링크를 보냈습니다").
    - 계정 없으면: PARTNER 계정 자동 provision(이메일 없이 전화 기반 — email은
      `kakao-{contact_id}@portal.local` 플레이스홀더, 발송은 알림톡) + 기본 이용권(§4-Q1).
  - **미승인(PENDING)** → "담당자 확인 후 안내드립니다" 응답 + 상담 관제 대기 뱃지에 노출
    (승인 화면에서 승인 → 자동으로 위 흐름 이어짐).
- 발급·재발급은 기존 감사·활동 이력 흐름 그대로 탄다.

### K2. 포털 안 '카카오 문의' — 여정 5번 연결
- PortalShell 헤더에 **[카카오톡 문의]** 버튼(카카오 옐로) — kakao_channel_url(챗 딥링크
  `.../chat`)로 새 탭. 사업 상세에는 "이 사업에 대해 문의" 링크(문의 시 사업명을 복사해
  붙여넣도록 프리셋 문구 클립보드 복사).
- 카카오 챗은 외부 앱이라 컨텍스트 자동 전달은 불가 — 복사 프리셋("[문의] {사업명} …")으로
  보완. 상담 관제 스레드는 이미 client 매칭돼 있어 담당자는 맥락 확인 가능.

### K3. 상담 → 이슈 승격 — 여정 7번 연결
- 상담 관제 스레드 상세에 **[이슈로 등록]** 버튼:
  - ActivityHistory(ISSUE, client_id=스레드 고객사, 담당=고객사 담당 PM 기본, 제목 자동
    "[상담] {고객사} 문의", 내용=최근 고객 메시지 요약 3개) 생성.
  - **thread↔issue 연결 저장**(ActivityHistory.related_history_id 대신 신규
    `chat_thread_id` 컬럼 additive) — 이슈 보드 카드에서 원 스레드로, 스레드에서 이슈로
    양방향 이동.
  - 스레드에 SYSTEM 메시지 "이슈로 등록됨(#…)" 적재, 모드는 WAITING 유지.
- 이슈 보드 카드에 💬 배지(연결 스레드 있음) → 클릭 시 상담 관제 해당 스레드.
- 이슈 CLOSED 시: 연결 스레드가 있으면 "고객에게 답변 보내기" 유도 배너(상담 관제로 이동).

### K4. 여정 마감 품질(선택)
- WAITING 24시간 무응답 스레드 자동 이슈 승격(배치) — monitor 후 결정.
- 여정 지표를 경영 관찰 운영 신호에 추가(상담→이슈 전환 수·평균 응답 시간).

## 3. 구현 순서·검증
K1(백엔드 웹훅+발급 재사용, 테스트: 승인/미승인/기존계정 분기) → K3(모델 additive+양방향
UI, 테스트: 승격·연결·CLOSED 배너) → K2(포털 버튼·프리셋, 프론트만) → K4(선택). 각 단계
pytest·vitest·빌드 후 로컬 커밋, dev 배포는 지시 시.

## 4. 결정 필요
| # | 질문 | 권고 |
|---|---|---|
| Q1 | 셀프 발급 기본 이용권 | **1개월권**(챗봇 자동 발급은 보수적으로 — 연장은 재요청) |
| Q2 | 자동 발급 대상 | **승인(APPROVED)+고객사 매칭 연락처만**(미승인은 담당자 승인 대기) |
| Q3 | 이슈 승격 담당자 | 고객사 담당 PM 기본(없으면 승격한 상담 담당) |
| Q4 | 24h 자동 승격(K4) | 보류 — K1~K3 정착 후 재논의 |
