# eTAS·BMS 원본 자동 정리·집계 체계 계획 (D6)

> 목표(사용자 정의): 담당자가 매월 eTAS·BMS에서 계약 운수사 원본(전월/특정월/기간)을 내려받아
> 수작업 엑셀로 정리하던 것을, **플랫폼에 원본을 올리거나 Dropbox에 저장하면 자동 정리 →
> 자동 집계(연평균) → 감축량 산정 입력**으로 잇는다.
>
> 작성: 2026-08-25. 상태: **계획만(미구현)**. 상위: DATA_DIGITIZATION_MASTER_PLAN(D5),
> CARBON_PARTICIPATION_LIFECYCLE_PLAN. ※ Docs 실데이터 — 구조만 기록.

---

## 0. 소스 실사(확인 완료)
- **raw eTAS**: 운수사별 월별 `.xls`(1,249개). 차량별 자동차등록번호·**운행일수·총 운행거리**·
  위험운전 등. 헤더 4~5행 복합, 데이터 6행~. = **운행(distance·days)**.
- **raw BMS/충전**: BMS취합(LONG: 운수사·차량번호·월·운행형태·운행거리·횟수), 충전량은 충전원천.
  = **충전량(kWh)** + 운행 보조.
- **담당자 취합본** `ALL_eTAS_Charge_Integrated`(WIDE): 차량×월 → 운행일수·운행거리·충전량(14개월).
  → 지금 **수작업으로 만드는 정본**. 자동화 대상.
- **베이스라인(내연)**: 3년 주행·주유 = **과거 고정값**(산정 등록 시 1회 확정, 유가보조금·과거 eTAS).
  **매월 크롤 대상 아님.** → 별도 1회 입력.
- 데이터 **부분성 확인**: 차량마다 운행 또는 충전 한쪽만 있는 경우 많음, 일부 프로그램 차량 미수집.

## 1. 핵심 구분 (체계의 축)
| 구분 | 성격 | 주기 | 소스 | CMS 저장 |
|---|---|---|---|---|
| **project(전기) 운행·충전** | 실측, 변동 | **매월** | eTAS·BMS·충전 원본 | 월별 로그 → 연평균 |
| **baseline(내연) 주행·연료** | 과거, 고정 | **1회** | 유가보조금·과거 eTAS(산정 등록) | 차량별 연평균(고정) |
자동화(업로드/Dropbox→정리→집계)는 **project 측**. baseline은 등록 시 1회 확정 입력.

## 2. 데이터 계층 설계
1. **원천 로그 `tb_vehicle_monthly_log`** (신규) — 차량번호·월(YYYY-MM)·지표(운행일수·운행거리·
   충전량·운행횟수)·**source(ETAS/BMS/CHARGE)**·운수사·업로드배치. **중복키 (차량번호, 월, source)**
   로 재업로드·기간중복 안전(upsert). 담당자 취합본을 이 로그가 대체.
2. **자동 정리(consolidate) 뷰** — 로그 → 차량×월 통합(운행일수·운행거리·충전량). = 담당자 WIDE
   취합본과 동일 화면(자동 생성). 결여(운행/충전 한쪽) 시각화.
3. **자동 집계(aggregate)** — 기간 선택 → 연평균: **project_distance=(Σ운행거리/Σ운행일수)×365,
   project_kwh=(Σ충전량/Σ운행일수)×365** (산정 시트 관례). → `VehicleCalcInput`의 project 측 채움.
4. **베이스라인 입력** — 내연 연평균 주행·주유를 **1회 업로드/입력**(등록 시 확정) → baseline 측.
5. **계산 연결** — 기존 검증 엔진(reduction_calc) → **3단계 감축량**:
   - **예상** = 등록 시 계획 project + baseline
   - **모니터링** = 매월 실측 로그 집계로 project 갱신 → 재계산(단계 stage 저장)
   - **최종** = 검증·발급 확정

## 3. 입력 경로 (3방식 병행)
- **A. 운수사별 월별 원본 직접 업로드**(.xls 다건) — eTAS·BMS 파서로 로그 적재. 담당자가 받은 원본 그대로.
- **B. Dropbox 폴더 자동 수집** — 지정 폴더(운수사/월)에 저장 → 스캔·파싱(세금계산서 Dropbox 스캔 관용구 재사용). "저장만 하면 자동 정리"의 핵심.
- **C. 담당자 취합본(WIDE) 업로드** — 과도기: 기존 방식 유지자용. 로그로 역-전개(월별 분해) 적재.

## 4. 파서 (실측 포맷 기준)
- **eTAS .xls**: 헤더 4~5행 스킵, 6행~ 데이터. 자동차등록번호·운행일수·총운행거리 추출.
  운수사·월은 파일명/상단 메타(회사명·운행기간)에서. `.xls`는 xlrd(읽기전용).
- **취합본(WIDE .xlsx)**: `YYYY년MM월_운행일수/운행거리/충전량` 3열 반복 → 월별 로그로 분해.
- **BMS/충전 LONG**: 차량번호·월·운행거리·충전량 직접 매핑.
- 차량번호로 **레지스트리(500대) 조인** → 프로그램 차량만 집계(전체 1만 대 중).

## 5. 부분 데이터·검증
- 운행 결여/충전 결여/운행일수 0 → "집계 불가" 플래그(계산 스킵, 목록에 사유).
- 이상치(운행거리/일수 비정상, 충전/km 이상) 경고. 월 수집 커버리지(대수/기대) 표시.
- 프로그램 차량 중 **미수집 차량 목록**(레지스트리 − 로그) → 담당자 수집 대상 안내.

## 6. 단계 로드맵
1. **P1 원천 로그 + 취합본(WIDE) 업로드** — `tb_vehicle_monthly_log` + WIDE 파서(담당자 정본 즉시 수용) + 자동 정리 뷰. (가장 빠른 가치 — 지금 취합본을 그대로 올림)
2. **P2 자동 집계 → 산정 입력** — 기간 집계 → project 연평균 → VehicleCalcInput 자동 채움 → 계산.
3. **P3 운수사별 원본(.xls) 직접 업로드** — eTAS/BMS raw 파서(취합 자동화, 담당자 수작업 제거).
4. **P4 Dropbox 자동 수집** — 폴더 저장 → 스캔·파싱(무업로드 자동화).
5. **P5 3단계 감축량 stage** — 예상/모니터링/최종 저장·비교(라이프사이클 연결).
각 단계 pytest·빌드·dev 검증 후 커밋. dev 전용(내년 데이터 고려한 실험적 구축).

## 7. 결정 필요
| # | 질문 | 권고 |
|---|---|---|
| E1 | 착수 지점 | **P1(취합본 WIDE 업로드+로그)** — 지금 담당자 정본을 즉시 수용, 빠른 가치 |
| E2 | baseline(내연) 입력 방식 | 차량별 연평균 **1회 업로드/입력**(등록 시 확정, 재계산 시 고정) |
| E3 | 연평균 산식 | **(Σ지표/Σ운행일수)×365** (산정 시트 관례) 확정 |
| E4 | 집계 대상 | 레지스트리 프로그램 차량(≈500)만. 전체 1만 대는 로그 보관·필터 |
| E5 | 충전량(kWh) 원천 | eTAS_Charge_Integrated의 충전 원천 확인 필요(BMS vs 충전기 API) |
| E6 | Dropbox 폴더 규칙 | 운수사/연월 경로 규칙 확정(기존 문서 아카이브 폴더 관용구 재사용) |

## 8. 원칙
- **자동화 대상은 project(월 변동)만** — baseline은 등록 시 고정.
- **원천 로그를 정본으로** — 담당자 수작업 엑셀을 CMS 로그가 대체, 취합본은 파생 뷰.
- **중복키(차량·월·출처)** 로 재업로드·기간중복 안전.
- **부분 데이터 투명화** — 결여·미수집·이상치를 숨기지 않고 플래그.
- 방법론 계산은 검증된 엔진 재사용(D5). raw→집계까지만 신규.
