# 운수사 계약대수 현황 관리 — 설계 계획서

> 목적: 운수사(고객사)의 **탄소배출권 계약대수 현황**을 매월 발행 엑셀(원본 탭)만 올리면
> 자동 반영하고, 수작업 관리(대상여부·계약여부 등)는 고객사 페이지에서, 집계는 통합 현황판
> 운수사 섹션에서 관리한다.
>
> 작성: 2026-08-20. 상태: **설계 확정(구현 전)**. 정본 엑셀:
> `Docs/고객사/탄소배출권 계약대수 현황(26.6월기준)_26.08.18.xlsx`(현황 542행 / 원본 549행)
> 관련: [[carbon-credit-cost-inventory-plan]] · 순수 CMS(운영 노출, 재무 은닉 아님)

---

## 0. 확정 결정 (사용자)
| 항목 | 결정 |
|---|---|
| 월별 데이터 | **월별 스냅샷 누적**(client×월 upsert, 추이 보존). 같은 월 재업로드는 덮어쓰기 |
| 매칭 기준 | **지역(조합)+회사명(정제)** → 고객사(TRANSPORT). 미매칭은 보류(client_id NULL) |
| 다중 사업장 행 | **client×월 합산**(같은 회사 여러 행은 한 행으로 합산) |
| 계약여부/조합계약/규제여부 | **Y/N** |
| 수작업 관리 편집 | **고객사 상세 '현황' 탭**(새 메뉴 아님 — 기존 6탭에 추가, 운수사만) |
| 원본 업로드 | **고객사 마스터 화면 버튼**('운수사 일괄 등록(표준)' 옆) — 새 메뉴 아님 |
| 집계 | **통합 현황판(대시보드) 운수사 섹션** — 기존 화면 내 위젯 |
| 노출 | **순수 CMS**(운영 노출) — 재무 은닉과 무관 |

> 메뉴 방침: **새 메뉴 0개**. 현황 탭은 `ClientDetailPage`의 TABS에 항목 1개 추가,
> 업로드는 `ClientsPage` 버튼, 집계는 `DashboardPage` 섹션 — 전부 기존 화면 확장.

---

## 1. 엑셀 구조 분석 (정본)
**두 탭 = 원본(자동) + 현황(수작업 가미)**:
- **원본 탭**(매월 발행 다운로드): 조합·업종(시내/농어촌/시외)·회사명·월·면허대수·계·차종별
  대수(경유·CNG·HB·전기·수소). 헤더가 2~4행 병합(복합) → **고정 컬럼 위치 매핑 파서** 필요.
- **현황 탭**(수작업 관리 뷰) = 원본 + 추가 컬럼:
  - `코드`(순번) · **`대상여부`(사업대상 489 / 규제대상 52)** · `계약여부` · `조합계약` · `규제여부`
  - 집계 섹션: 전체 현황 / 외부사업 대상(할당·목표 제외) / 외부사업 미계약 현황
- 월(F)=현재 파일은 6월 단일. 같은 회사 다중 행 존재(사업장 분할 — 금강고속 4행 등).

**핵심 함의**: 원본(차량대수)과 수작업(대상/계약 상태)을 **분리 저장**해야 매월 원본 재업로드가
수작업을 덮지 않는다.

---

## 2. 데이터 모델 (신규)

### 2.1 `tb_fleet_status` — 월별 대수 스냅샷(원본 유래, 업로드로 갱신)
- `fleet_status_id`(PK), `client_id`(FK→tb_client, **nullable**=미매칭 보류, SET NULL)
- `region`(조합) · `industry`(업종: 시내/농어촌/시외) · `company_name`(원본 회사명 원문)
- `period`(YYYY-MM, 예 '2026-06') · `license_count`(면허대수) · `total_count`(계)
- 차종별: `diesel`(경유) · `cng` · `hybrid`(HB) · `electric`(전기) · `hydrogen`(수소)
- `source`(EXCEL) · `created_by` · `created_at`
- **UNIQUE(client_id, period, company_name)** — 매칭 건(client_id 有) upsert 유일.
  ⚠️ SQL상 NULL≠NULL이라 **미매칭(client_id NULL)은 DB 유니크가 안 걸림 → 앱 레벨 dedup**
  (region+company_name+period)을 F2 파서에서 처리해야 함.
- 다중 사업장(같은 회사 여러 행)은 업로드 시 **합산**해 1행(확정).
- 인덱스: (period), (client_id), (region)

### 2.2 `tb_fleet_mgmt` — 수작업 관리(고객사 단위, 업로드 무영향)
- `client_id`(PK/FK, 1:1) · `target_type`(대상여부: 사업대상/규제대상, tb_code) ·
  `contract_yn`(계약여부) · `union_contract`(조합계약) · `regulated_yn`(규제여부) ·
  `memo` · `updated_by` · `updated_at`
- 원본 업로드는 이 테이블을 **건드리지 않음**(수작업 보존).

### 2.3 tb_code 신규 카테고리
- `FLEET_TARGET`(사업대상/규제대상) · `FLEET_INDUSTRY`(시내/농어촌/시외) — 하드코딩 금지 규약.
- 계약여부/조합계약/규제여부는 Y/N(기존 yn 관용구) 또는 코드 — §5.

---

## 3. 업로드 파이프라인 (원본 탭)
- 표준 import_spec은 라벨 매칭이라 **복합 헤더(2~4행 병합)에 부적합** → 원본 탭 전용 파서
  (`services/fleet_import.py`, 고정 컬럼 매핑 + 데이터 시작행 자동 감지).
- 회사명 정제는 기존 `excel_import._tf_company_clean` 재사용(괄호 제거 등).
- **매칭**: (지역 정규화 + 정제 회사명) → tb_client(TRANSPORT). 미매칭 → client_id NULL 보류.
- **period**: 업로드 시 사용자가 대상 월(YYYY-MM) 지정(파일에 연도 없음 — 기본은 파일명/현재연+월).
- **미리보기 → 적용**(다른 import과 동일): 행별 {매칭/미매칭·신규/갱신} 표 → 확정 시 upsert.
- 라우터: `POST /fleet-status/preview` · `/commit`(월 파라미터), 권한 master.write.

## 4. 화면
### 4.1 고객사 페이지 '현황' 탭 (신규)
- **월별 대수 추이**: 그 고객사의 tb_fleet_status를 period 내림차순 — 면허대수·차종 구성(전기 비중)
  ·전월 대비 증감. (차종 전환 = 경유/CNG→전기·수소 추이)
- **수작업 관리 편집**: tb_fleet_mgmt 편집 폼(대상여부·계약여부·조합계약·규제여부·비고). 저장은
  업로드와 독립.

### 4.2 통합 현황판 운수사 섹션 (신규 위젯)
- 최신 월 집계: 총 운수사 수 · 총 면허대수 · **차종 구성(전기 대수·비중)** · 대상여부별 카운트
  (사업대상/규제대상) · 계약/미계약 카운트 · **전월 대비 전기 증감**.
- 지역(조합)·업종 분포 미니 요약.

## 5. 단계(초안, 각 4원칙 루프)
- ✅ **F1** 데이터모델: tb_fleet_status·tb_fleet_mgmt + ensure_schema(인덱스·유니크) +
  tb_code FLEET_TARGET/FLEET_INDUSTRY 시드. 스키마·라운드트립 테스트. (커밋 208a74d)
- ✅ **F2** 원본 파서(services/fleet_import.py, 고정 컬럼 A조합 B업종 C회사명 E월 F면허 G계
  H경유 I CNG J HB K전기 L수소, '원본' 탭 우선) + 업로드 preview/commit(지역+정제회사명
  매칭·다중 사업장 합산·period upsert·미매칭 앱-dedup) + 현황/수작업 API
  (routers/fleet_status.py). 실파일 545행 파싱 검증·pytest 5종. (커밋 a7187bf)
- ✅ **F3** 고객사 '계약대수 현황' 탭(운수사만): 월별 추이 조회 + 수작업 관리 편집. 고객사
  마스터 화면 '계약대수 현황 업로드' 버튼(월 선택+미리보기). (커밋 0ac60b9)
- ✅ **F4** 대시보드 운수사 섹션(GET /dashboard/fleet): 최신 월 집계·전월 대비 전기·대상/
  계약 카운트·업종 분포. (커밋 6536809)
- ⏳ **F5** 검증: **dev 배포 후** 실파일(2탭 정본) 업로드 e2e — 실제 운수사 마스터와의 매칭률·
  집계·재업로드(수작업 보존)·미매칭 보류 확인. 로컬은 파서·합산·upsert·집계 pytest 완료.

## 6. 미결(착수 시 확정)
1. ✅ **다중 사업장 행**: **client×period 합산**(확정).
2. **period 연도**: 업로드 월 지정 UI(기본값 파일명/현재). 파일에 연도 없음.
3. ✅ **계약여부/조합계약/규제여부**: **Y/N**(확정).
4. ✅ 현황 탭: **운수사(TRANSPORT)만**(확정).
5. 원본 파서 컬럼 매핑 확정(2~4행 병합 헤더 실측 후 고정 위치표) — F2 착수 시.

## 7. 재사용 체크리스트
- 회사명 정제 `_tf_company_clean` · 지역 매칭(정규화) · import preview/commit 패턴 ·
  tb_code 시드(main.seed_codes) · ensure_schema 컬럼/인덱스 · 대시보드 stats 스키마 확장.

## 8. 다음 액션
1. 사용자: §6 미결(특히 다중 사업장 합산·period·계약여부 형식) 확인.
2. 개발: F1(모델)부터 4원칙 루프. 각 증분 로컬 커밋, 배포는 "배포" 명시 시.
