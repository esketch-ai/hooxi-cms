# 운수사 버스 감축 참여 라이프사이클 — 직관적 관리 계획

> 목표(사용자 정의): 운수사별로 버스가 **신규도입·대체도입**으로 감축사업에
> **참여했거나(기참여)·참여중이거나·향후 참여해야 할** 것을 구분하고, **예상 감축량 →
> 모니터링 감축량 → 최종 감축량(사업 승인 완료)** 3단계를 **한눈에 파악·관리**한다.
>
> 작성: 2026-08-25. 상태: **계획만(미구현)**. 관련: 부록 L(감축량·지급 파생),
> asset-mgmt-plan, fleet-status-feature, project-finance-expansion.

---

## 0. 성공 기준
- 운수사 하나를 열면, **그 운수사의 버스들이 참여 상태별(기참여/참여중/미참여)로 분류**되고,
  각 버스의 **도입구분(신규/대체)** 과 **3단계 감축량**이 한 화면에서 보인다.
- 사업 진행(기획→등록→모니터링→검증→발급)과 **감축량 3단계가 연결**돼 진행률이 직관적으로 보인다.
- "아직 사업에 안 넣은 대상 버스(향후 참여)"를 골라볼 수 있어 **참여 확대 관리**가 된다.
- 전 운수사 크로스 관점에서도 같은 축(참여상태·도입구분·3단계 감축량)으로 집계·검색된다.

## 1. 현행 실사 — 있는 것 / 빠진 것
### 데이터(모델)
- **ProjectVehicle(참여 차량)**: `introduction_type`(신규/대체) · 연차 `reduction_y1~y10` ·
  `total_reduction`(**예상 감축량**=연차 단순합, 방법론 산정 ingest) · `effective_reduction`
  (**승인 후 잔여반영감축량**, 파생) · `expected_payout` · `client_vehicle_id`(보유차량 링크).
- **Project(감축사업)**: `project_status`(기획/등록완료/모니터링/검증/발급완료) ·
  `approval_status`(미승인/승인) · `expected_credits` · `issued_credits`(**확정 발급량**) · `issued_at`.
- **ClientVehicle(보유 차량 원장)**: 운수사 보유 버스 전량 · `status`(운행/폐차) · 차대/차명/연식.
  (도입구분 없음 — 도입구분은 '참여' 속성이라 ProjectVehicle에만.)
- **FleetStatus/FleetMgmt**: 월별 계약대수·현황 분류(대상/계약/규제). 대수 관점(감축량 아님).

### 화면
- **전기버스 자산(/asset-vehicles)**: 크로스-프로젝트 차량 리스트 + 필터(사업·운수사·승인상태·
  등록일·만료). `total_reduction`(10년 총감축량)·`effective_reduction`·`expected_payout` 노출.
  → **재무·차량 중심**. 필터에 **도입구분·참여상태 없음**, "미참여" 개념 없음(이미 참여한 것만).
- **감축 사업 관리(/projects)**: 사업 상세에 참여 차량(VehicleFormModal). → **사업 중심**(운수사 아님).
- **고객사 상세**: 탭 개요/이력/보고서/자산/**보유 차량**/**계약대수 현황**/상담. → 보유차량·대수는
  있으나 **감축 참여·감축량 라이프사이클 탭 없음**.
- **자산관리 보고(/asset-report)**: 운수사×사업 정산 요약(금액 중심).

### 결론 — 3가지가 빈다
1. **운수사 중심 라이프사이클 통합뷰 없음** — 사업중심·차량크로스·재무중심만 있고, "운수사 A의
   버스별 참여 상태 + 3단계 감축량"을 한눈에 보는 화면이 없다.
2. **모니터링 감축량 단계 부재** — 예상(total_reduction)·승인후(effective_reduction)는 있으나
   **모니터링 기간 실측/검증전 감축량 필드가 없다**(3단계 중 가운데가 빔).
3. **참여 상태 분류(기참여/참여중/미참여) 부재** — ClientVehicle↔ProjectVehicle 연결로 암묵적일
   뿐, 상태로 분류·필터하거나 "미참여 향후대상"을 골라보는 수단이 없다.

## 2. 3단계 감축량 — 정의·데이터 설계
| 단계 | 정의 | 현행 | 필요 |
|---|---|---|---|
| **예상 감축량** | 방법론 산정(연차합) — 참여 확정 시 | `ProjectVehicle.total_reduction` | 있음(라벨만 '예상'으로 명확화) |
| **모니터링 감축량** | 모니터링 기간 실측/검증전 추정 | **없음** | **신규**: `ProjectVehicle.monitored_reduction`(+측정기간·차수) 또는 사업 모니터링 감축량 → 차량 배분 |
| **최종 감축량** | 사업 승인·발급 확정 | `effective_reduction`(파생) / `Project.issued_credits`(사업) | **명확화**: 차량별 '최종 확정 감축량'을 발급량 배분/effective로 확정·동결 |

- 3단계는 **동일 차량에 대해 시점별 값**이라 나란히 비교(예상 대비 모니터링 대비 최종)해 오차·달성률을 본다.
- 모니터링 감축량 **출처 결정 필요**(방법론 재ingest / 수기 입력 / 파일). §5-D1.

## 3. 참여 상태 — 파생 분류(저장 안 함)
버스(ClientVehicle) × 참여(ProjectVehicle) × 사업(Project.status) 조합으로 파생:
- **기참여(완료)**: 참여 사업이 **발급완료**(크레딧 확정). 최종 감축량 존재.
- **참여중(진행)**: 참여 사업이 **기획~검증**(발급 전). 예상·모니터링 감축량 관리 대상.
- **미참여(향후대상)**: 보유버스 중 **어떤 ProjectVehicle에도 안 걸린** 버스. 대상차종/전기버스
  필터로 "참여시켜야 할 후보"로 노출. (정의 §5-D3)
- 한 버스가 여러 사업 이력을 가질 수 있음(대체도입 등) → 최신 참여 기준 상태 + 이력 타임라인.

## 4. 화면안 — 직관적 관리
### A. 운수사 중심 '감축 참여' 탭 (딥다이브) — 권장 주(主)
- 고객사(운수사) 상세에 **'감축 참여' 탭 신설**. 구성:
  - 상단 요약: **참여율**(참여/보유), 상태별 대수(기·현·미), **3단계 감축량 합계**(예상/모니터링/최종).
  - 버스 리스트: 차량번호·도입구분 배지·**참여 상태 배지**·소속 사업·사업단계·**3단계 감축량**(나란히)
    ·달성률(모니터링/예상, 최종/예상). 상태·도입구분·사업 필터.
  - **미참여 탭/섹션**: 후보 버스 목록 → 사업 참여 추가 유도(기존 ProjectVehicle 추가 흐름 연결).
  - 시각화: 버스별 진행 파이프라인(도입→참여→모니터링→승인→발급)·3단계 감축량 진행 바.
### B. 전 운수사 크로스 확장 — 보(補)
- 전기버스 자산(/asset-vehicles)에 **도입구분·참여상태 필터 + 3단계 감축량 컬럼** 추가.
- 대시보드/자산관리 보고에 상태별·단계별 집계(운수사별 참여율·예상↔최종 오차) 신호.
### C. 공통
- 도입구분(신규/대체)은 공통코드 VEHICLE_INTRO 재사용. 참여상태는 **파생**(코드 불필요) 또는
  경량 코드 PARTICIPATION_STATUS 신설(라벨·색 관리 원하면).

## 5. 결정 필요(착수 전)
| # | 질문 | 권고 |
|---|---|---|
| D1 | **모니터링 감축량 출처** | 방법론 스프레드시트 재ingest(예상과 동일 경로) 우선 — 수기 보정 허용 |
| D2 | **최종 감축량 정의** | 사업 발급량(issued_credits)을 차량 effective 비율로 배분·동결 |
| D3 | **미참여(향후대상) 정의** | 보유버스 중 미참여 + (전기버스/대상차종) — 대상 필터 확정 필요 |
| D4 | **화면 위치** | **운수사 상세 '감축 참여' 탭(주) + asset-vehicles 확장(보)** 둘 다 |
| D5 | 참여상태 표현 | 우선 **파생**(저장X). 라벨·색 커스터마이즈 필요 시 코드화 |
| D6 | 감축량 단위·표기 | tCO₂, 소수 2자리(기존 Num 규격 재사용) |

## 6. 단계(구현 시)
1. **P1 참여상태 파생 + 표시(모델 무변경, 즉효)** — ClientVehicle↔ProjectVehicle↔Project.status로
   기/현/미 파생. asset-vehicles·운수사 상세에 상태·도입구분 노출. "미참여 후보" 조회.
2. **P2 모니터링 감축량 모델** — `ProjectVehicle.monitored_reduction`(+측정 메타) additive(ensure_schema).
   입력/재ingest 경로(D1). 예상↔모니터링 비교.
3. **P3 운수사 '감축 참여' 탭** — 요약·리스트·미참여·시각화(3단계·파이프라인).
4. **P4 최종 감축량 확정·배분(D2)** + 크로스 집계·신호(보).
각 단계 pytest·빌드·dev 검증 후 로컬 커밋, 지시 시 배포.

## 7. 원칙
- **운수사 관점을 1급으로** — 지금은 사업/재무 중심. 운수사별 참여·감축 라이프사이클을 정본 뷰로.
- **파생 우선·모델 최소 추가** — 참여상태는 파생, 신규 컬럼은 모니터링 감축량 등 꼭 필요한 것만.
- **방법론은 외부 산정** — CMS는 결과(감축량)를 ingest·관리(부록 G/L 원칙 유지), 재계산 안 함.
- **3단계 나란히** — 예상↔모니터링↔최종을 비교해 오차·달성률로 관리 신뢰도를 높인다.
