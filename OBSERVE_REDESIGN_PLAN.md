# 경영 관찰(/observe) 전면 재설계 계획 — Executive View

> 문제의식(사용자): 현행 화면은 KPI 카드 나열(운영 4 + 재무 5 + 감축 4) + 지연 목록뿐 —
> **그래프·추이·경영 판단**을 위한 뷰로 너무 약하다. 역할도 불분명하다.
>
> 작성: 2026-08-21. 상태: **설계안(구현 전)**. 대상 사용자: 경영진·경영전략실(OBSERVER,
> '경영진'·'경영전략실' 접근 그룹).

---

## 0. 역할 재정의

| | 통합 현황판(/dashboard) | **경영 관찰(재설계)** |
|---|---|---|
| 질문 | "오늘 무엇을 해야 하나" | **"회사가 어디로 가고 있나"** |
| 시간축 | 오늘·이번 달 | **월 추이(기본 12개월)·전월 대비** |
| 단위 | 건(액션) | **원·톤·%·건(집계)** |
| 사용자 | 실무자·팀장 | 경영진·경영전략실 |

경영 관찰 = **돈의 흐름 · 사업 진행 · 자산 전환 · 리스크**를 추이로 보여 의사결정을
돕는 화면. 읽기 전용 유지.

### 0.1 인터랙션 원칙 — 3단계 프로세스 (사용자 확정)
1. **한눈(Glance)**: 화면을 열면 스크롤 1~2회 안에 전사 상황이 파악된다 — 숫자·추이·경고 배지.
2. **개요(Overview) — 클릭 드릴다운**: 모든 KPI·차트 요소·퍼널 단계는 클릭하면 **그 자리에서
   개요 드로어**가 열린다. 화면 이동 없음 — OBSERVER는 실무 화면 접근이 차단돼 있으므로
   드릴다운은 반드시 자체 개요로 완결한다. 개요 = 상위 구성 목록(최대 10~20행) + 합계 +
   한 줄 해설(이 숫자가 어떻게 나왔는지).
3. **질문(Ask)**: 개요의 각 행에 **담당자 이름**을 표기 — "더 궁금하면 누구에게 물어볼지"가
   개요에서 끝난다. (연락 액션은 두지 않음 — 대면/메신저는 회사 프로세스)

## 1. 활용 가능한 시계열 데이터 (실사 — 전부 이미 DB에 있음)

| 소스 | 시간축 | 만들 수 있는 추이 |
|---|---|---|
| tb_tax_invoice (issue_date·공급가액·매입/매출) | 발행일 | **월별 매출·매입·마진** |
| tb_settlement (confirmed/billed/completed_at·금액) | 상태시각 | **월별 확정·청구·입금 + 파이프라인 잔량(미수)** |
| tb_market_rate (effective_date·unit_price) | 유효일 | **탄소배출권 시세 추이** |
| finance_query(보유수량) × 시세 | — | 재고평가 현재+추이(시세 변동분) |
| tb_fleet_status (period·전기/경유…) | 월 | **전기 전환율 추이**(운수사 계약대수) |
| tb_report_delivery (period·status) | 월 | 보고서 발송률 추이 |
| tb_activity_history (activity_date·type) | 일 | 월별 활동량·이슈 발생/종결 |
| tb_project (approval_status·stage) | — | 사업 상태 분포·지연(기존 위젯) |

## 2. 화면 구성안 (위→아래)

1. **KPI 스트립(6개, 전월 대비 Δ 배지)** — 매출인식 · 매출이익 · 재고평가 ·
   미수금(청구 후 미입금) · 예상 지급액 · 관리 고객사. 숫자는 Num 규격(소수·단위 축소).
2. **재무 추이(12개월 콤보 차트)** — 막대: 월별 매출/매입(세금계산서 공급가액),
   선: 정산 입금액. 호버 툴팁·최근 월 강조.
3. **정산 파이프라인(퍼널)** — 예정→확정→청구→입금 단계별 건수·금액과 단계 체류
   잔량(병목). "청구 후 30일 경과 미입금 N건" 리스크 배지.
4. **탄소배출권 패널** — 시세 추이 라인(등록 이력 전체) + 보유/매각 수량 도넛 +
   재고평가(보유×현재시세)와 6개월 평균가 비교.
5. **전기 전환 추이** — 운수사 계약대수의 월별 전기 비중 라인 + 최신 월 지역 상위
   5(막대). (계약대수 데이터가 쌓일수록 선이 길어짐)
6. **사업 단계** — 상태 분포(도넛: 기획/추진/승인/…) + 기존 지연·임박 목록(유지).
7. **운영 신호(소형 3종)** — 보고서 발송률(월별 %），미처리 긴급 이슈 수，월별 활동량
   (유형 스택 막대).

모바일: 섹션 세로 스택, 차트 가로 스크롤 없음(반응형 SVG).

### 2.1 개요 드로어 명세 (클릭 대상 → 개요 내용, 전부 담당자 표기)
| 클릭 | 개요 드로어 내용 |
|---|---|
| KPI 매출인식/매출이익 | 최근 월 매출 세금계산서 상위 10(상대처·공급가액·사업·발행일) + 산식 해설 |
| KPI 재고평가 | 사업별 보유 수량×현재시세 상위 10 + 시세 기준일 |
| KPI 미수금 | 청구 후 미입금 건 전체(운수사·사업·확정액·청구일·경과일·**담당자**) — 경과일 내림차순 |
| KPI 예상 지급액 | 운수사별 예상지급 상위 10(+담당자) |
| 재무 추이의 월 막대 | 그 달 개요: 매출/매입 합계·상위 계산서 5·입금 내역 |
| 퍼널 각 단계 | 단계 체류 건 목록(운수사×사업·금액·해당 시점·**담당자**) |
| 시세 라인 | 최근 등록 이력 10(유효일·단가·등록자) |
| 전기 전환 라인/지역 막대 | 그 월 지역별 표(면허·전기·비중) — 대시보드 6표 재사용 |
| 사업 분포 도넛 조각 | 해당 상태 사업 목록(사업명·차량수·**담당 PM**) |
| 운영 신호 각각 | 해당 월 건 목록(제목·고객사·담당자) |

- 개요 API: `GET /observe/detail?topic=<kpi|month|funnel|rate|ev|project|signal>&key=…`
  — topic별 커서 없는 상한(20행) 목록 + 합계 + 해설 텍스트. OBSERVER_SCOPE 등록.
- 드로어는 공용 `components/Drawer` 재사용(이미 존재), 로딩 스켈레톤·빈 상태 포함.

## 3. 기술 방침

- **차트: 의존성 추가 없이 자체 SVG 프리미티브** — `components/charts/`에
  `TrendChart`(라인+막대 콤보)·`Donut`·`MiniBars` 3종(~200줄). 현재 번들에 차트
  라이브러리가 없고(실사), recharts 도입은 +90kB gz·테마 정합 비용 — 필요 지표가
  단순(라인/막대/도넛)해 자체 구현이 유지보수에 유리. 색은 기존 시맨틱 팔레트.
- **백엔드: `GET /observe/summary` 단일 엔드포인트** — KPI+Δ, 시계열 배열들(months[],
  revenue[], purchase[], paid[], funnel, rates[], ev_share[], report_rate[], activity[])
  를 1콜로. 월 그룹핑은 방언 의존 없는 파이썬 집계(행수 수천 이하 — fleet-tables 선례).
  OBSERVER_SCOPE_EXACT에 추가, '경영진/경영전략실' 그룹 레지스트리(/observe)에 매핑.
- 민감 금액은 기존 observe 관성 유지(SensitiveData 마스킹 토글 대상: 매출·이익·미수).
- 재무 OFF 빌드: 현행처럼 /observe 자체가 은닉 경로 — 변화 없음.

## 4. 단계(4원칙 루프)

- **OB-R1** 백엔드 `/observe/summary`(집계+Δ) + `/observe/detail`(개요 드로어 데이터,
  담당자 이름 포함) + tests. OBSERVER 스코프 등록.
- **OB-R2** 차트 프리미티브 3종(SVG, 반응형·다크/라이트 대응) + KPI 스트립·재무 추이
  섹션으로 페이지 골격 교체.
- **OB-R3** 정산 퍼널·탄소 패널·전기 전환·사업 분포·운영 신호 섹션 + 전 요소 개요
  드로어 연결(클릭 가능성이 보이는 hover 처리).
- **OB-R4** 사용자 가이드(observe 토픽) 재작성 · vitest/빌드 · dev 배포 후 실데이터 검증
  (dev에 세금계산서·정산·계약대수·시세 데이터가 이미 있어 즉시 검증 가능).

## 5. 결정 필요
| # | 질문 | 권고 |
|---|---|---|
| Q1 | 추이 기본 기간 | 12개월(토글 6/12/24) |
| Q2 | 금액 마스킹 | 매출·이익·미수는 SensitiveData(클릭 노출) — 회의실 화면 보호 |
| Q3 | 차트 방식 | 자체 SVG(무의존) — §3 근거 |
| Q4 | 기존 KPI 카드 | 전부 제거하지 않고 KPI 스트립 6개로 통폐합 |
